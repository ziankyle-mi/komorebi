# Locally-installed agent skill bundles — never commit to this repo
.agents/
skills-lock.json

# node / npm
node_modules/
*.tgz
npm-debug.log*

# Local-only demos (IP) — never commit
examples/apple-home/

# Personal, machine-local preferences — never commit
CLAUDE.local.md

# OS / editor noise
.DS_Store
*.swp
.idea/
.vscode/
dist/
