<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/notif_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/widget_bg"
    android:orientation="vertical"
    android:padding="12dp">

    <!-- 1. Header Row: Avatar + Partner Status + Energy Pill -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/notif_avatar"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_launcher" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="9dp"
            android:layout_marginEnd="8dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/notif_partner_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="Mikkie • Joyful"
                android:textColor="#FFFFFF"
                android:textSize="12sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/notif_time_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="1dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="✦ Komorebi Sanctuary • Live ⚡"
                android:textColor="#A1A1AA"
                android:textSize="9.5sp" />
        </LinearLayout>

        <TextView
            android:id="@+id/notif_energy_pill"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/widget_pill_bg"
            android:paddingHorizontal="8dp"
            android:paddingVertical="3dp"
            android:text="⚡ 100%"
            android:textColor="#F8CF65"
            android:textSize="9.5sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- 2. Daily Whisper Note Card -->
    <LinearLayout
        android:id="@+id/notif_whisper_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:background="@drawable/notif_note_bg"
        android:orientation="vertical"
        android:padding="8dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="💌 DAILY NOTE"
            android:textColor="#F8CF65"
            android:textSize="8.5sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/notif_whisper_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:ellipsize="end"
            android:maxLines="3"
            android:text="Thinking of you today! 🌸"
            android:textColor="#FFFFFF"
            android:textSize="11.5sp" />
    </LinearLayout>

    <!-- 3. Shared Photo Container (Hidden by default, shown ONLY when photo exists) -->
    <FrameLayout
        android:id="@+id/notif_photo_container"
        android:layout_width="match_parent"
        android:layout_height="140dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/widget_snap_bg"
        android:padding="2dp"
        android:visibility="gone">

        <ImageView
            android:id="@+id/notif_photo_img"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop" />
    </FrameLayout>

    <!-- 4. Quick Action Buttons Bar -->
    <LinearLayout
        android:id="@+id/notif_actions_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/notif_btn_sanctuary"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/notif_btn_primary_bg"
            android:gravity="center"
            android:paddingVertical="6dp"
            android:text="🌸 View Sanctuary"
            android:textColor="#F8CF65"
            android:textSize="10sp"
            android:textStyle="bold" />

        <View
            android:layout_width="6dp"
            android:layout_height="1dp" />

        <TextView
            android:id="@+id/notif_btn_chat"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/notif_btn_secondary_bg"
            android:gravity="center"
            android:paddingVertical="6dp"
            android:text="💬 Open Chat"
            android:textColor="#FFFFFF"
            android:textSize="10sp"
            android:textStyle="bold" />
    </LinearLayout>

</LinearLayout>
